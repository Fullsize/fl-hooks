# useAxios

基于axios的react hooks 封装，使用方法如axios一样

```javascript
import React from "react";
import { useAxios } from "../src/index";
const Page = () => {
  useAxios({
    url: "/123",
  });
  return <code></code>;
};
export default Page;
```
